- name: Install ansible collection 
  set_fact:
    PATH: "{{ lookup('env', 'PATH') }}/usr/local/bin:{{ ansible_env.PATH }}" 

- name:  
  community.general.ansible_galaxy_install:
  type: collections
  name: community.windows


#      - community.general
#      - ansible.posix
#      - community.windows
#      - community.vmware
#      - community.crypto
#    executable: "{{ ansible_env.PATH }}/ansible-galaxy"

